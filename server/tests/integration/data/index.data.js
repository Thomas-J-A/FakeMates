const users = require('./user.data');
const posts = require('./post.data');
const comments = require('./comment.data');
const advertisements = require('./advertisement.data');

module.exports = {
  users,
  posts,
  comments,
  advertisements,
};
